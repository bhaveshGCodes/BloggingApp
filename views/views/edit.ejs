<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap Link (Dn) only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style type="text/css">
        .parent {
            display: flex;
            flex-direction: column;
            /* border: 2px solid red; */
            min-height: 100vh;
        }

        .child[data-order='3'] {
            /*  flex dosent have justify-self, so we can use margin instead to put it at the bottom  */
            margin-top: auto;
        }

        a:link {
            text-decoration: none;
        }
    </style>

    <title>Edit Article | Blog App</title>
</head>

<body>
    <!-- this is edit.ejs -->

    <div class="parent">
        <div class="container">
            <h1 class="mb-4">Edit</h1>
            <form action="/edit" method="POST">

                <div class="form-group">
                    <label for="title">Title</label>
                    <input required type="text" value="<%=articles.title %>" name="title" id="title"
                        class="form-control mb-4">
                </div>
                <div class="form-group">
                    <label for="description">Description</label>
                    <textarea name="description" id="description" value="HELLO"
                        class="form-control mb-4"><%=articles.description %></textarea>
                </div>
                <div class="form-group">
                    <label for="markdown">Text</label>
                    <textarea required name="markdown" id="markdown" value="<%=articles.markdown %>"
                        class="form-control mb-4"><%=articles.markdown %></textarea>
                </div>

                <div>
                    <input hidden required type="text" name="id" value=<%=articles._id %>>
                </div>


                <a href="/" class="btn btn-secondary mt-2">Cancel</a>

                <button class="btn btn-primary mt-2">Save</button>

            </form>
        </div>
        <%- include('_footer.ejs') %>
    </div>
</body>

</html>